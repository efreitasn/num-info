language: node_js
node_js:
  - node
cache:
  yarn: true
branches:
  only:
    - master
script: yarn build
deploy:
  provider: pages
  local-dir: build
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true